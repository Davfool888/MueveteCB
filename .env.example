# Configuración del agente Eco. Copia estas claves a .env.local; nunca subas el archivo real.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5
# En Vercel dev local, usa true para probar /api/chat desde el navegador.
VITE_CHAT_API=false
